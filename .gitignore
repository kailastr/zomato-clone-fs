
/server/node_modules
/server/.env

# we dont need to upload dist file because every time "npm run start" command runs, the dist is created
/server/dist 

# an error may occur in src/api/auth/index.js - /google/callback

/client/package-lock.json